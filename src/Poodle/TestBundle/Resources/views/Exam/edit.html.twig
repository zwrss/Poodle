{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Exam edit</h1>

    {{ form(edit_form) }}
    
    <ul>
    {% for question in entity.questions %}
        <li>{{ question.body }} [ <a href="{{ path("test_exam_add_answer", { 'questionId' : question.id }) }}">dodaj odpowiedź</a> ]</li>
    {% endfor %}
</ul>

    <a href="{{ path('test_exam_add_question', { 'examId': entity.id}) }}" id="add-another-question">Dodaj nowe pytanie</a>

    
    
        <ul class="record_actions">
    <li>
        <a href="{{ path('test_exam') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
        
{% endblock %}
